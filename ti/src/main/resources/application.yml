spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/course
    username: postgres
    password: pgadmin
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create # validate | update | create | create-drop
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
  servlet:
      context-path: /courses-ti